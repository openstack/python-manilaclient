---
fixes:
  - |
    Fixed a bug where the ``properties`` field in several show commands
    (share show, share snapshot show, share replica show,
    share network subnet show, and share export location show)
    was unconditionally wrapped in ``DictColumn``, causing a JSON
    serialization error when using the ``-f json`` formatter.
